#pragma once

// AetherMesh firmware version, reported in Telemetry.firmware_version.
// Keep within Telemetry.firmware_version max_size (12 chars incl. null) — see proto/mesh.options.
#define AETHERMESH_FW_VERSION "1.2.0"

# AetherMesh release versions — mirrored in VERSION at repo root for CI validation.
versionName=1.3.7
versionCode=16

# Optional CARTO basemap key (free): https://carto.com/basemaps/apikey/
cartoApiKey=

org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=512m -Dfile.encoding=UTF-8
# Keep the debug APK after connectedAndroidTest so field phones are not wiped.
android.injected.androidTest.leaveApksInstalledAfterRun=true
